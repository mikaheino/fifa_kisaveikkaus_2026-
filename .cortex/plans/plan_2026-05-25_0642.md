---
created: 2026-05-25T06:42:43.478Z
session: eb7ac9ff-2b6c-405d-8962-f7cb17b6fdde
working_directory: /Users/mika.heino/prod/fifa_kisaveikkaus_2026
---

## Deployment Plan — First-time FIFA-veikkaus 2026

### Context
- Database `STREAMLIT_APPS` exists, but `FIFA_VEIKKAUS` schema does not
- No warehouse, roles, tables, stage, or app exist yet
- Current connection can USE ROLE ACCOUNTADMIN
- All 46 source files are ready in the local repo

### Implementation Steps

1. **Run setup.sql as ACCOUNTADMIN** (executed via sql_execute in sections):
   - Create warehouse `FIFA_VEIKKAUS_WH`
   - Create schema `STREAMLIT_APPS.FIFA_VEIKKAUS`
   - Create roles (`FIFA_VEIKKAUS_PLAYER_ROLE`, `FIFA_VEIKKAUS_ADMIN_ROLE`)
   - Create tables (SCHEDULE, RESULTS, PREDICTIONS, PLAYOFF_PREDICTIONS, PLAYOFF_RESULTS)
   - Create view `FIFA_VEIKKAUS_RESULTS_V`
   - Create stage `FIFA_VEIKKAUS_STAGE`
   - Apply all grants
   - Seed 72-game schedule via MERGE
   - Grant roles to user `mika.heino@recordlydata.com` (admin role)

2. **Upload source files to stage** via PUT commands:
   - `streamlit_app.py`, `schedule_data.py`, `environment.yml`
   - `app_pages/*.py` (all page modules + components)
   - `data/schedule_2026.json`
   - `assets/` (logo_2026.png, maradona.gif, pirlo.gif)

3. **Create the Streamlit app object** pointing at the stage

4. **Run sanity checks** — verify 72 schedule rows, 12 groups, 17 match days

5. **Verify the app URL** via SHOW STREAMLITS

### Verification
- SELECT COUNT(*) on schedule = 72
- SHOW STREAMLITS confirms app is created
- App URL accessible in browser

### Critical Files
- `db/setup.sql` — full bootstrap DDL+DML
- `streamlit_app.py` — production entry point
- `environment.yml` — runtime dependencies
- `data/schedule_2026.json` — schedule source of truth
- `schedule_data.py` — Python data loader